service:
  title: HawkerFlow
  root_path: '/customer'
  port: 8080
  host: '127.0.0.1'
  allow_origins:
    - '*'
  methods:
    - '*'
  headers:
    - '*'
  docs_url: '/docs'
  redoc_url: '/redoc'
  reload: true
  scheme: 'http'
  credentials: true

datasource:
  driver:
    package: "drivers.sqlite_driver"
    driver_class: "SQLiteDriver"
  database:
    connection_url: 'sqlite:///database.db'
    driver_name: 'postgresql+psycopg2'
    name: 'hawker_flow_customer'
    host: 'localhost'
    port: 5432
  options:
    echo: false